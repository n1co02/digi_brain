//Author: Nico Mangold

//defined searchParams for routes

export const enum RoomListSearchParams {
  UserId = 'userId',
}

export const enum RoomSearchParams {
  UserId = 'userId',
  RoomId = 'roomId',
}
